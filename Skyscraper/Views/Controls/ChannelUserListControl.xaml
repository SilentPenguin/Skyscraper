<UserControl 
    x:Class="Skyscraper.Views.Controls.ChannelUserListControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Skyscraper"
    xmlns:models="clr-namespace:Skyscraper.Data;assembly=Skyscraper.Data"
    xmlns:viewModels="clr-namespace:Skyscraper.ViewModels"
        
    mc:Ignorable="d"
        
    d:DataContext="{d:DesignInstance Type=models:Channel, IsDesignTimeCreatable=True}"
    d:DesignHeight="300" d:DesignWidth="300">
             
    <UserControl.Resources>
        
        <DataTemplate DataType="{x:Type models:User}">
            <TextBlock Foreground="Black">
                <Run Text="{Binding Path=Nickname}" />
            </TextBlock>
        </DataTemplate>
        
    </UserControl.Resources>
    
    <Grid>
        <ListView ItemsSource="{Binding Path=Users}" />
    </Grid>
    
</UserControl>
